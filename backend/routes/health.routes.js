// import express from "express";
// import { getHealthData, addHealthData, updateHealthData, deleteHealthData } from "../controllers/health.controller.js";
// import { authenticateToken } from "../middleware/auth.middleware.js";

// const router = express.Router();

// // Routes pour les données de santé, toutes protégées par JWT
// router.get("/", authenticateToken, getHealthData);
// router.post("/", authenticateToken, addHealthData);
// router.put("/:id", authenticateToken, updateHealthData);
// router.delete("/:id", authenticateToken, deleteHealthData);

// export default router;
